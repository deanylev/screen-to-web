{
  "name": "screen-to-web",
  "version": "1.0.0",
  "description": "Output your Mac's screen over HTTP using MJPEG",
  "license": "MIT",
  "scripts": {
    "clean": "node-gyp clean",
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "start": "node index.js"
  },
  "dependencies": {
    "command-line-args": "^5.2.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "node-addon-api": "^5.0.0",
    "node-gyp": "^9.3.0"
  }
}
